- name: Add host to /etc/hosts
  become: yes
  lineinfile:
    path: /etc/hosts
    state: present
    regexp: "{{ libvirt_vm_name }}"
    line: "{{ hostvars[{{ libvirt_vm_name}}]['ansible_default_ipv4']['address'] }} {{ libvirt_vm_name}}"
